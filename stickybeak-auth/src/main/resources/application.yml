server:
  port: 8081

spring:
  application:
    name: stickybeak-auth
  config:
    import:
      - optional:nacos:stickybeak-auth.yml
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDR:localhost:8848}
      config:
        server-addr: ${NACOS_ADDR:localhost:8848}
        file-extension: yml

management:
  endpoints:
    web:
      exposure:
        include: health,info
