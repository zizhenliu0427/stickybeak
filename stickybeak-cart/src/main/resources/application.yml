server:
  port: 8083

spring:
  application:
    name: stickybeak-cart
  config:
    import:
      - optional:nacos:stickybeak-cart.yml
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDR:localhost:8848}
      config:
        server-addr: ${NACOS_ADDR:localhost:8848}
        file-extension: yml

management:
  endpoints:
    web:
      exposure:
        include: health,info
