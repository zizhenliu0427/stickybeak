server:
  port: 8086

spring:
  application:
    name: stickybeak-notification
  config:
    import:
      - optional:nacos:stickybeak-notification.yml
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDR:localhost:8848}
      config:
        server-addr: ${NACOS_ADDR:localhost:8848}
        file-extension: yml

management:
  endpoints:
    web:
      exposure:
        include: health,info
